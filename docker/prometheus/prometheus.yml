# Prometheus configuration stub — fully configured in Task 08
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: bff
    static_configs:
      - targets: ['bff:4000']
    metrics_path: /metrics
